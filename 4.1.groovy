// 4.1 Объявить пустой список, словарь. Обратиться к первому элементу(в случае словаря обратиться по любому ключу).
def emptyList = []  
def map = [:]

println (emptyList[0]) 
println (map[])
println (map[0])
println (map)